---
site:
  domain: sample.com
  # subdomain: test
  # alias: domain.com
  root: httpdocs
  username: sample
  caracal_branch: v0.5
  # ip_address: 0.0.0.0
  # owner: Some Person
  # plan: Unlimited
  # database_type: 'mysql'
  # lets_encrypt: 'person@host.com'

local_site_directory: ../site
subscription_directory: "{{ plesk_vhost_directory }}/{{ site.domain }}"
site_directory: "{{ subscription_directory }}/{{ site.root }}"
